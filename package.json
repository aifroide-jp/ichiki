{
  "name": "mockup2wp",
  "version": "0.3.0",
  "description": "Ichiki Phase 0 — scan a mockup into acf-map.yaml and a per-case CLAUDE.md (deterministic, no LLM)",
  "bin": { "mockup2wp": "bin/mockup2wp.js" },
  "scripts": {
    "scan": "node bin/mockup2wp.js scan",
    "demo": "node bin/mockup2wp.js scan fixture --out out --project nature-kitakyushu",
    "test": "node test/run.js",
    "test:update": "node test/run.js --update"
  },
  "dependencies": {
    "cheerio": "1.0.0",
    "js-yaml": "4.1.0"
  },
  "license": "UNLICENSED"
}
